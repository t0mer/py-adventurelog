# Copy to `.env` and fill in. `.env` is git-ignored; this file is not.
ADVENTURELOG_BASE_URL=
ADVENTURELOG_USERNAME=
ADVENTURELOG_PASSWORD=
# Populated automatically after first login (optional token cache):
ADVENTURELOG_SESSION_TOKEN=
